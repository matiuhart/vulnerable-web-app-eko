---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vulnerable-app
  labels:
    app: vulnerable-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vulnerable-app
  template:
    metadata:
      labels:
        app: vulnerable-app
    spec:
      containers:
      - name: vulnerable-app
        image: ghcr.io/matiuhart/vulnerable-web-app-eko:sha-27e06103f5c0354cbf4b7b8acf89c9d1b4eb62ad
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: NODE_ENV
          value: "production"
---
---
apiVersion: v1
kind: Service
metadata:
  name: vulnerable-app-svc
  labels:
    app: vulnerable-app
spec:
  type: NodePort
  selector:
    app: vulnerable-app
  ports:
    - port: 9080
      targetPort: 8080
      protocol: TCP
      name: http
      nodePort: 30080